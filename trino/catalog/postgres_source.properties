# ── Trino Catalog: PostgreSQL Source ─────────────────────────
# This connector federates queries to the user-managed PostgreSQL source database.
# Trino will introspect schemas and tables dynamically.
connector.name=postgresql
connection-url=jdbc:postgresql://postgres-source:5432/source_db
connection-user=source_user
connection-password=source_pass_2024

